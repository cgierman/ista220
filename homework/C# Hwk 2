# Chance Gierman

### 16Jan2020

1. Variables that exist only in method or another section of code.

2. a command that performs an action, such as calulating a value and storing the result or displaying a message to the user.

3. Names that you use to identify the elements in your programs, such as namespaces, classes, methods, and variables.

4. A storage location that holds value.

5. A code block that contains a series of statements. A program causes the statements to be executed by calling the method and specifying any required method arguments.

6. No, A value type is ussually whatever type reside on the stack. A primitive type is a type defined at the programming language level, often it is even a value type.

7. It can "operate" on values to create new values.

8. Call the ToString Method.

9.  Call the system.Int32.Parse method.

10. Precedence governs the order and Associativity determines the direction. example: 2+3*4, if Associativity took over the answer would 20 but precedence says that multiplication is preformed first making the answer 14.

11. That you must assign a value to a variable before you can use it.

12. prefix is evaluated before the expression and postfix is evaluated after the expression

13. A feature added to C# is string interpolation, which renders many uses of the + operator obsolete for concentenating strings.  

14. The types of values that you assign to the variable cannot change from the used to initialize the variable.